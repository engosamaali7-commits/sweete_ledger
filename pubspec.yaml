name: sweets_ledger
description: دفتر إلكتروني لإدارة مبيعات الحلويات والسمبوسة عبر المحافظ
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  sqflite: ^2.3.0
  path: ^1.8.3
  path_provider: ^2.1.1
  intl: ^0.19.0
  share_plus: ^7.2.1
  printing: ^5.11.1
  pdf: ^3.10.7
  url_launcher: ^6.2.2
  shared_preferences: ^2.2.2
  open_file: ^3.3.2          # لفتح ملفات PDF

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true